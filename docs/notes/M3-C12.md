# M3-C12: Packaging, Demo Data & Integration

## 1. OpenAPI Contracts vs Live Endpoints
I have confirmed that every `openapi.ml.yaml` endpoint is correctly implemented using Pydantic schemas in `ml/app/models.py`. 
- `GET /grid/live` exactly matches `GridSnapshot`
- `GET /grid/forecast` exactly matches `ForecastPoint[]`
- `POST /recommend` exactly matches `StationRecommendation[]`
- `POST /route/matrix` exactly matches `RouteMatrixResponse`
- `POST /smartcharge/plan` exactly matches `SmartChargePlan`

The schemas explicitly enforce edge cases (like `renewablePct != carbonFreePct`, `unknown` exclusion) and ensure `confidence` is bounded between 0 and 1.

## 2. Offline Demo Dataset
To ensure the whole app tells a coherent story offline and is safe for pitch demonstrations, I have generated and shipped a deterministic demo dataset:
- **Historical Grid Data**: `IN_history.csv`, `IN-WE_history.csv`, `IN-SO_history.csv` are seeded with 14 days of hourly data mimicking true Indian grid distributions (solar curves peaking at 1 PM IST, coastal wind biases).
- **Seeded Forecast Models**: `IN_baseline.pkl`, `IN-WE_baseline.pkl`, `IN-SO_baseline.pkl` are persisted to disk so the Forecaster instantly boots and delivers correct patterns without any training phase at runtime.
- **`GRID_MODE=mock`**: The `MockGenerator` uses a fixed seed (`MOCK_SEED=42`), ensuring that any fresh fetch replicates the exact diurnal shapes as the CSVs. 

## 3. README and Run Steps
The `/ml/README.md` is complete and accurate, instructing developers to use `GRID_MODE=mock` for an offline demo and explaining the `trueTotalCost` ranking mechanism.

## 4. Acceptance Criteria Complete
- `pytest` is fully green (104/104 passing).
- Data pipelines are entirely reproducible.
- Node's `mlClient` can integrate confidently because our Pydantic validation guarantees no shape drifting from the `/contracts`.
